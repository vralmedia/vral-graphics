# Release handoff — 2026-08-26

Local funnel rebuild. **Not published.**

- Clone: this repository
- Remote: `git@github.com:vralmedia/vral-graphics.git`
- Branch: `main`
- Start HEAD: `fe97917efcc1504d36575af63c6c861c02377cf3`
- Push: no
- Domain / Lovable: not touched

## How to look

```
npm test
npm run dev
```

Open `http://127.0.0.1:4173/`

Public routes: `/`, `/offers/`, `/quote/`.  
Private routes: `/field/`, `/admin/` (no homepage link).

Backend on `:8787` through the Vite `/api` proxy. Public intake is `POST /api/print-requests` with no browser Bearer. Protected `POST /api/leads` stays server-to-server.

## Money and delivery

See `PAYMENT-MATRIX.md`. QuickBooks stays BLOCKED without OAuth. CRM and flyer email stay BLOCKED without operator URLs. Admin never marks Paid until a verified payment webhook.

## Do not

- Push
- Publish the domain
- Connect Lovable until João asks
- Put `VG_FORM_API_KEY` in the browser

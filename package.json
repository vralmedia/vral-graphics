{
  "name": "vral-graphics",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html styles.css app.js vral-v.png dist/ && cp -R assets dist/assets"
  }
}